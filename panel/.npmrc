# Панель GML использует react 19, а часть @radix-ui пакетов декларирует
# peer-зависимость react 16-18. Апстрим Gml.Web.Client собирается с
# legacy-peer-deps — без этого npm ci падает с ERESOLVE.
legacy-peer-deps=true
